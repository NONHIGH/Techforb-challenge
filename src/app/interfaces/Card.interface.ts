export interface Card {
  balance: number;
  dueDate: Date;
  headline: string;
  numberCard: number;
  securityCode: string;
}

export interface CardDTO {
  balance: number;
  dueDate: Date;
  headline: string;
  numberCard: number;
}
